export const sum = (prev: number, current: number) => prev + current
export const max = (prev: number, current: number) => Math.max(current, prev)
export const min = (prev: number, current: number) => Math.min(current, prev)
export const multiply = (prev: number, current: number) => prev * current
